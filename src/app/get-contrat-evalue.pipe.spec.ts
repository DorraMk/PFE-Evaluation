import { GetContratEvaluePipe } from './get-contrat-evalue.pipe';

describe('GetContratEvaluePipe', () => {
  it('create an instance', () => {
    const pipe = new GetContratEvaluePipe();
    expect(pipe).toBeTruthy();
  });
});
