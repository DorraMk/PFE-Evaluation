import { GetSessionPipe } from './get-session.pipe';

describe('GetSessionPipe', () => {
  it('create an instance', () => {
    const pipe = new GetSessionPipe();
    expect(pipe).toBeTruthy();
  });
});
