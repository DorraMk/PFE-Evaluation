import { GetEvaluePipe } from './get-evalue.pipe';

describe('GetEvaluePipe', () => {
  it('create an instance', () => {
    const pipe = new GetEvaluePipe();
    expect(pipe).toBeTruthy();
  });
});
