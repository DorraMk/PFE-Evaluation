import { ContratsBySessionPipe } from './contrats-by-session.pipe';

describe('ContratsBySessionPipe', () => {
  it('create an instance', () => {
    const pipe = new ContratsBySessionPipe();
    expect(pipe).toBeTruthy();
  });
});
