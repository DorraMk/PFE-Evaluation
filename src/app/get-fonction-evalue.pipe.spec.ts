import { GetFonctionEvaluePipe } from './get-fonction-evalue.pipe';

describe('GetFonctionEvaluePipe', () => {
  it('create an instance', () => {
    const pipe = new GetFonctionEvaluePipe();
    expect(pipe).toBeTruthy();
  });
});
