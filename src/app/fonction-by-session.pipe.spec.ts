import { FonctionBySessionPipe } from './fonction-by-session.pipe';

describe('FonctionBySessionPipe', () => {
  it('create an instance', () => {
    const pipe = new FonctionBySessionPipe();
    expect(pipe).toBeTruthy();
  });
});
