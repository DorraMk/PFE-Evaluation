import { PipemethodsPipe } from './pipemethods.pipe';

describe('PipemethodsPipe', () => {
  it('create an instance', () => {
    const pipe = new PipemethodsPipe();
    expect(pipe).toBeTruthy();
  });
});
