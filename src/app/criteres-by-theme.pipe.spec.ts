import { CriteresByThemePipe } from './criteres-by-theme.pipe';

describe('CriteresByThemePipe', () => {
  it('create an instance', () => {
    const pipe = new CriteresByThemePipe();
    expect(pipe).toBeTruthy();
  });
});
