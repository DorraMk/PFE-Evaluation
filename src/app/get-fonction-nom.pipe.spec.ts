import { GetFonctionNomPipe } from './get-fonction-nom.pipe';

describe('GetFonctionNomPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFonctionNomPipe();
    expect(pipe).toBeTruthy();
  });
});
